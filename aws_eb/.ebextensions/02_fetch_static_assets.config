# Commands run before the application and the web server are set up.
commands:
  # Fetch all front end assets from S3.
  01_fetch_static_assets:
    command: aws s3 cp --recursive s3://inquests.ca-frontend/assets /var/www/
